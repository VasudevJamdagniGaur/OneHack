# Minimum stack for the Phase 1 sample pipeline and tests.
# Geospatial libraries are listed but commented until real rasters are downloaded.
# Do not install rasterio/geopandas until the region and real-data download are confirmed.

numpy>=1.26
pandas>=2.2
PyYAML>=6.0
matplotlib>=3.8
pytest>=8.0

# Prithvi flood detector (separate install; ~1 GB weights):
#   pip install -r requirements-prithvi.txt
#   python scripts/run_prithvi_flood.py --download

# Planned after real data is available (Windows + Python 3.13 wheels can be fragile):
# scikit-learn>=1.5
# joblib>=1.4
# rasterio>=1.3
# geopandas>=1.0
# shapely>=2.0
# xarray>=2024.1
# rioxarray>=0.15
# folium>=0.16
# plotly>=5.20
