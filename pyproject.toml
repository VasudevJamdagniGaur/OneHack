[project]
name = "flood-ai"
version = "0.1.0"
description = "Satellite-based spatial flood-risk early-warning prototype"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
