# Runtime for ibm-nasa-geospatial/Prithvi-EO-2.0-300M-TL-Sen1Floods11.
# The model card pins terratorch==0.99.8. That pin forces NumPy 1.26, which
# has no Windows wheel for Python 3.13. terratorch 1.2.x keeps the same
# BACKBONE_REGISTRY.build API and supports Python 3.13.
#
#   pip install -r requirements-prithvi.txt
#   python scripts/run_prithvi_flood.py --download

torch
torchvision
timm>=1.0.15
einops
terratorch>=1.2.13
huggingface_hub
rasterio
