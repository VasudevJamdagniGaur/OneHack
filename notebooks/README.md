# Notebooks

Exploration notebooks will go here after the region is confirmed and a real clipped subset exists.

Phase 1 uses `scripts/run_sample_pipeline.py` instead of a notebook so the first milestone stays scriptable and testable.
